# Core Telegram bot framework.
# This project is written for aiogram v2 (uses Dispatcher(bot), @dp.message_handler,
# @dp.callback_query_handler, dp.start_polling(reset_webhook=..., timeout=..., relax=...),
# etc.). aiogram v3 is a breaking rewrite and will NOT work without a full migration.
aiogram>=2.25,<3

# aiogram 2.x requires aiohttp 3.8.x; keep them aligned.
aiohttp>=3.8,<4

requests>=2.28
faker>=18.0
beautifulsoup4>=4.11
numpy>=1.23
flask>=2.2
playwright>=1.40
urllib3>=1.26
